(require 'cc-mode)

(setq-default c-basic-offset 2)
(setq-default c-default-style '((java-mode . "java")
                                (awk-mode . "awk")
                                (other . "k&r")))

(provide 'conf-c)
